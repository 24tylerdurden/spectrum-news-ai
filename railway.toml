[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python main.py"
healthcheckPath = "/"
healthcheckTimeout = 300

[[services]]
name = "news-pipeline"
env = ["PYTHONUNBUFFERED=1"]
