
services:
  news-pipeline:
    build: .
    container_name: biased-india-pipeline
    restart: unless-stopped
    env_file:
      - .env.example
    volumes:
      - ./logs:/app/logs
      - models_cache:/app/models_cache

volumes:
  models_cache:
